class users {
    constructor(user_id, user_name, password, nickname,createdAt) {
        this.user_id = user_id;
        this.user_name = user_name;
        this.password = password;
        this.nickname = nickname;
        this.createdAt = createdAt; // 생성 시점의 타임스탬프
    }
}